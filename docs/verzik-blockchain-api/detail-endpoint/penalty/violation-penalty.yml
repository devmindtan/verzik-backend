info:
  name: violation-penalty
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/violation-penalty?tenantId={{tenant_id}}&violationCode=SPAM"
  params:
    - name: tenantId
      value: "{{tenant_id}}"
      type: query
    - name: violationCode
      value: SPAM
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
