info:
  name: cosign-policy
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/cosign-policy?tenantId={{tenant_id}}&docType=1"
  params:
    - name: tenantId
      value: "{{tenant_id}}"
      type: query
    - name: docType
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
