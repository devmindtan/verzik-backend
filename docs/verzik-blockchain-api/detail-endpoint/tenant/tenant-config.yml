info:
  name: tenant-config
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/tenant-config?id={{tenant_id}}"
  params:
    - name: id
      value: "{{tenant_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
