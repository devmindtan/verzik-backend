info:
  name: tenant
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/tenant?id={{tenant_id}}"
  params:
    - name: id
      value: "{{tenant_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
