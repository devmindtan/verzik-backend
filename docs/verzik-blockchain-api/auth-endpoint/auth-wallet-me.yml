info:
  name: auth/wallet/me
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/auth/wallet/me"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
