info:
  name: get-tenant-info
  type: http
  seq: 23

http:
  method: GET
  url: "{{base_url}}/tenant-info?tenantId={{tenant_id}}"
  params:
    - name: tenantId
      value: "{{tenant_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
