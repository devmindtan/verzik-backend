info:
  name: get-cosign-policies
  type: http
  seq: 16

http:
  method: GET
  url: "{{base_url}}/cosign-policies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
