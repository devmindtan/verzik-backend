info:
  name: get-tenant-count
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/tenant-count"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
