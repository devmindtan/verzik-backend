info:
  name: get-operator-soft-slasheds
  type: http
  seq: 18

http:
  method: GET
  url: "{{base_url}}/operator-soft-slasheds"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
