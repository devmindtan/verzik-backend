info:
  name: get-document-qualifieds
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/document-qualifieds"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
