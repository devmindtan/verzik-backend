info:
  name: get-operator-hard-slasheds
  type: http
  seq: 17

http:
  method: GET
  url: "{{base_url}}/operator-hard-slasheds"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
