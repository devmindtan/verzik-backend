info:
  name: get-tenant-runtime-config
  type: http
  seq: 21

http:
  method: GET
  url: "{{base_url}}/tenant-runtime-config?tenantId={{tenant_id}}"
  params:
    - name: tenantId
      value: "{{tenant_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
