info:
  name: get-operator-unstakeds
  type: http
  seq: 20

http:
  method: GET
  url: "{{base_url}}/operator-unstakeds"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
