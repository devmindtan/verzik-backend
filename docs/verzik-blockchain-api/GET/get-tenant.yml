info:
  name: get-tenant
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/tenant?tenantId={{tenant_id}}"
  params:
    - name: tenantId
      value: "{{tenant_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
