info:
  name: get-operator-unstake-requesteds
  type: http
  seq: 19

http:
  method: GET
  url: "{{base_url}}/operator-unstake-requesteds"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
