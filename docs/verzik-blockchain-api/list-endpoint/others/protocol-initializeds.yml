info:
  name: protocol-initializeds
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/protocol-initializeds"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
