info:
  name: cosign-policies
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/cosign-policies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
