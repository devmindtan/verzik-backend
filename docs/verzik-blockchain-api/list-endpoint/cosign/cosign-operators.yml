info:
  name: cosign-operators
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/cosign-operators"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
