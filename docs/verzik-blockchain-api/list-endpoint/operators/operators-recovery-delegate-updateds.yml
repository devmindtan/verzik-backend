info:
  name: operators/recovery-delegate-updateds
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/operators/recovery-delegate-updateds"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
