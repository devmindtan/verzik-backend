info:
  name: operators/recovereds
  type: http
  seq: 10

http:
  method: GET
  url: "{{base_url}}/operators/recovereds"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
