info:
  name: operators/stake-topped-ups
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/operators/stake-topped-ups"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
