info:
  name: operators/soft-slashed
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/operators/soft-slashed"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
