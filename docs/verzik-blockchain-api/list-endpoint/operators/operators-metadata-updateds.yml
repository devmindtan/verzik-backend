info:
  name: operators/metadata-updateds
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/operators/metadata-updateds"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
