info:
  name: operators/unstaked
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/operators/unstaked"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
