info:
  name: operators/unstake-requested
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/operators/unstake-requested"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
