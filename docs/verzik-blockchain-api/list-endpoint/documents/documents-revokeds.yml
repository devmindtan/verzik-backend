info:
  name: documents/revokeds
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/documents/revokeds"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
