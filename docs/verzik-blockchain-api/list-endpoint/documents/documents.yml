info:
  name: documents
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/documents"
  auth: inherit

runtime:
  variables:
    - name: ""
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
