info:
  name: documents/cosigneds
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}/documents/cosigneds"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
