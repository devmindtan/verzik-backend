info:
  name: tenants/status-updateds
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/tenants/status-updateds"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
