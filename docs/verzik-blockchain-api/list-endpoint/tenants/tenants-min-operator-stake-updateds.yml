info:
  name: tenants/min-operator-stake-updateds
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/tenants/min-operator-stake-updateds"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
