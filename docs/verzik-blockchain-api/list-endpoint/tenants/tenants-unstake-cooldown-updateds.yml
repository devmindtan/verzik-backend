info:
  name: tenants/unstake-cooldown-updateds
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/tenants/unstake-cooldown-updateds"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
