info:
  name: tenants/treasury-updateds
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/tenants/treasury-updateds"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
